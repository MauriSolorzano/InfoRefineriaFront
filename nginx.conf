server {
    listen 80;
    server_name localhost;

    location / {
        root   /usr/share/nginx/html;
        # Aquí le decimos que busque login.html como prioridad
        index  login.html index.html;
        try_files $uri $uri/ /login.html;
    }
}